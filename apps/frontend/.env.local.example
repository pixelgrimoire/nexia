# Variables de entorno frontend

# URL del API Gateway para rewrites locales (opcional)
# Si no se define, en Docker usará http://api-gateway:8000
# En desarrollo local puedes establecer:
# API_GATEWAY_URL=http://localhost:8000
API_GATEWAY_URL=
